<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <name>patient-diagnostic-assistant</name>
  <url>https://github.com/pprds-public/patient-diagnostic-assistant/</url>

  <organization>
    <name>PPRD Systems, LLC</name>
    <url>https://pprdsystems.com/agentic-ai</url>
  </organization>

  <licenses>
    <license>
      <name/>
      <url/>
      <distribution/>
    </license>
  </licenses>

  <developers>
    <developer>
      <email>mohammed@pprdsystems.com</email>
      <name>Mohammed El Bahja</name>
      <url>https://pprdsystems.com/agentic-ai</url>
    </developer>
  </developers>

  <groupId>com.pprdsystems.ai</groupId>
  <artifactId>patient-diagnostic-assistant</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>pom</packaging>

  <properties>
    <useIncrementalCompilation>false</useIncrementalCompilation>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <maven.build.timestamp.format>MM-dd-yyyy_HHmm</maven.build.timestamp.format>
    <maven.test.skip>false</maven.test.skip>

    <java.version>17</java.version>
    <scala.version>2.13</scala.version>
    <flink.version>2.0.0</flink.version>

    <startCommand-class>com.pprdsystems.ai.agentic.prompts.chaining.workflow.normalizer</startCommand-class>

    <!-- plugins -->
    <maven-clean-plugin.version>3.1.0</maven-clean-plugin.version>
    <maven-license-plugin.version>3.1.0</maven-license-plugin.version>
    <maven-directory-plugin.version>0.3.1</maven-directory-plugin.version>
    <maven-dependency-plugin.version>3.2.0</maven-dependency-plugin.version>
    <maven-compiler-plugin.version>3.8.1</maven-compiler-plugin.version>
    <maven-install-plugin.version>2.5.2</maven-install-plugin.version>
    <maven-surefire-plugin.version>2.22.2</maven-surefire-plugin.version>
    <maven-exec-plugin.version>3.0.0</maven-exec-plugin.version>
    <maven-deploy-plugin.version>3.0.0-M1</maven-deploy-plugin.version>
    <maven-resources-plugin.version>3.1.0</maven-resources-plugin.version>
    <javadoc-plugin.version>3.2.0</javadoc-plugin.version>

    <!-- logging versions -->
    <log4j.version>1.2.17</log4j.version>
    <slf4j.version>2.0.9</slf4j.version>
    <logback.version>1.4.11</logback.version>

    <!-- apache commons versions -->
    <commons-logging.version>1.2</commons-logging.version>
    <commons-lang.version>2.6</commons-lang.version>
    <commons-lang3.version>3.12.0</commons-lang3.version>
    <commons-io.version>2.11.0</commons-io.version>
    <commons-cli.version>1.5.0</commons-cli.version>
    <commons-validator.version>1.7</commons-validator.version>
    <commons-collections.version>3.2.2</commons-collections.version>
    <commons-collections4.version>4.4</commons-collections4.version>
    <commons-codec.version>1.13</commons-codec.version>
    <commons-text.version>1.8</commons-text.version>
    <commons-compress.version>1.19</commons-compress.version>

    <!-- Spring -->
    <spring.version>6.0.12</spring.version>
    <spring-boot.version>3.1.4</spring-boot.version>

    <jackson.version>2.18.1</jackson.version>

    <!-- Test Libraries -->
    <junit-platform.version>1.8.1</junit-platform.version>
    <junit-jupiter.version>5.8.1</junit-jupiter.version>
    <junit-platform-surefire-provider.version>1.3.2</junit-platform-surefire-provider.version>
    <junit-platform.version>1.8.1</junit-platform.version>
    <junit-jupiter.version>5.8.1</junit-jupiter.version>
    <junit-platform-surefire-provider.version>1.3.2</junit-platform-surefire-provider.version>
    <junit.version>4.12</junit.version>

    <jacoco.minimum-coverage>0.0</jacoco.minimum-coverage>
  </properties>

  <repositories>
    <repository>
      <id>maven-central1</id>
      <url>https://repo1.maven.org/maven2</url>
    </repository>
    <repository>
      <id>sonatype-releases</id>
      <url>https://oss.sonatype.org/content/repositories/releases/</url>
    </repository>
    <repository>
      <id>mvn-repository</id>
      <url>https://mvnrepository.com/artifact/</url>
    </repository>
    <repository>
      <id>confluent</id>
      <url>https://packages.confluent.io/maven/</url>
    </repository>
    <repository>
      <id>akka</id>
      <url>https://repo.akka.io/maven</url>
    </repository>
  </repositories>

  <modules>
    <module>parent-poms/pom.xml</module>
  </modules>

  <dependencyManagement>
    <dependencies>

      <!-- apache commons -->
      <dependency>
        <groupId>commons-logging</groupId>
        <artifactId>commons-logging</artifactId>
        <version>${commons-logging.version}</version>
      </dependency>

      <dependency>
        <groupId>org.apache.commons</groupId>
        <artifactId>commons-lang3</artifactId>
        <version>${commons-lang3.version}</version>
      </dependency>

      <dependency>
        <groupId>commons-io</groupId>
        <artifactId>commons-io</artifactId>
        <version>${commons-io.version}</version>
      </dependency>

      <dependency>
        <groupId>commons-cli</groupId>
        <artifactId>commons-cli</artifactId>
        <version>${commons-cli.version}</version>
      </dependency>

      <!-- logging -->
      <dependency>
        <groupId>ch.qos.logback</groupId>
        <artifactId>logback-core</artifactId>
        <version>${logback.version}</version>
      </dependency>

      <dependency>
        <groupId>org.slf4j</groupId>
        <artifactId>slf4j-api</artifactId>
        <version>${slf4j.version}</version>
      </dependency>

      <dependency>
        <groupId>ch.qos.logback</groupId>
        <artifactId>logback-classic</artifactId>
        <version>${logback.version}</version>
      </dependency>

      <dependency>
        <groupId>javax.inject</groupId>
        <artifactId>javax.inject</artifactId>
        <version>1</version>
      </dependency>

      <!-- Guice -->
      <dependency>
        <groupId>com.google.inject</groupId>
        <artifactId>guice</artifactId>
        <version>7.0.0</version>
      </dependency>

      <dependency>
        <groupId>com.google.inject.extensions</groupId>
        <artifactId>guice-assistedinject</artifactId>
        <version>7.0.0</version>
      </dependency>

      <!-- jackson dependencies, imported as a BOM -->
      <dependency>
        <groupId>com.fasterxml.jackson</groupId>
        <artifactId>jackson-bom</artifactId>
        <version>${jackson.version}</version>
        <scope>import</scope>
        <type>pom</type>
      </dependency>

      <!-- Kafka client API -->
      <dependency>
        <groupId>org.apache.kafka</groupId>
        <artifactId>kafka-clients</artifactId>
        <version>3.7.0</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <directory>target</directory>
    <sourceDirectory>src/main/java</sourceDirectory>
    <testSourceDirectory>src/test/java</testSourceDirectory>
    <outputDirectory>target/classes</outputDirectory>
    <testOutputDirectory>target/test-classes</testOutputDirectory>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
        <includes>
          <include>**/*.properties</include>
          <include>**/*.yml</include>
          <include>**/*.yaml</include>
        </includes>
      </resource>
      <resource>
        <directory>src/main/schema</directory>
        <targetPath>schema</targetPath>
      </resource>
    </resources>
    <testResources>
      <testResource>
        <directory>src/test/resources</directory>
        <includes>
          <include>**/*.properties</include>
          <include>**/*.yml</include>
          <include>**/*.yaml</include>
        </includes>
      </testResource>
    </testResources>

    <pluginManagement>
      <plugins>
        <!-- maven-clean-plugin -->
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-clean-plugin</artifactId>
          <version>${maven-clean-plugin.version}</version>
          <executions>
            <execution>
              <id>clean-dependencies</id>
              <phase>clean</phase>
              <inherited>false</inherited>
              <configuration>
                <failOnError>true</failOnError>
                <filesets>
                  <fileset>
                    <directory>${project.build.directory}</directory>
                  </fileset>
                  <fileset>
                    <directory>${project.build.directory}/lib</directory>
                  </fileset>
                </filesets>
              </configuration>
              <goals>
                <goal>clean</goal>
              </goals>
            </execution>
          </executions>
        </plugin>

        <!-- maven-dependency-plugin -->
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-dependency-plugin</artifactId>
          <version>${maven-dependency-plugin.version}</version>
          <executions>
            <execution>
              <id>copy-dependencies</id>
              <phase>prepare-package</phase>
              <goals>
                <goal>copy-dependencies</goal>
              </goals>
              <configuration>
                <outputDirectory>${project.build.directory}/lib</outputDirectory>
                <overWriteReleases>true</overWriteReleases>
                <overWriteSnapshots>true</overWriteSnapshots>
                <overWriteIfNewer>true</overWriteIfNewer>
                <includeScope>runtime</includeScope>
              </configuration>
            </execution>
          </executions>
        </plugin>

        <!-- maven-compiler-plugin -->
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven-compiler-plugin.version}</version>
          <configuration>
            <encoding>${project.build.sourceEncoding}</encoding>
            <release>${java.version}</release>
            <source>${java.version}</source>
            <target>${java.version}</target>
            <parameters>true</parameters>
            <showDeprecation>true</showDeprecation>
            <showWarnings>true</showWarnings>
            <failOnWarning>true</failOnWarning>
          </configuration>
          <executions>
            <execution>
              <configuration>
                <compilerArgs>
                  <arg>-Xlint:all,-options,-deprecation</arg>
                  <arg>-Xdoclint:none</arg>
                  <arg>-XDcompilePolicy=simple</arg>
                </compilerArgs>
              </configuration>
              <id>default-testCompile</id>
              <phase>test-compile</phase>
            </execution>
          </executions>
        </plugin>

        <!-- maven-install-plugin  -->
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-install-plugin</artifactId>
          <version>${maven-install-plugin.version}</version>
        </plugin>

        <!-- exec-maven-plugin -->
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>exec-maven-plugin</artifactId>
          <version>${maven-exec-plugin.version}</version>
          <executions>
            <execution>
              <goals>
                <goal>exec</goal>
              </goals>
            </execution>
          </executions>
          <configuration>
            <skip>true</skip>
            <executable>java</executable>
            <!-- <mainClass>${startCommand-class}</mainClass> -->
            <arguments/>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-clean-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-install-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>